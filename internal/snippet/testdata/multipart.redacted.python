import os

import requests

response = requests.request(
    "POST",
    "http://localhost:8080/upload",
    auth=(os.environ["APIC_USER"], os.environ["APIC_PASSWORD"]),
    data={
        "title": "***",
    },
    files={
        "file": ("q3.pdf", open("docs/report.pdf", "rb"), "application/pdf"),
    },
)
print(response.status_code)
print(response.text)
