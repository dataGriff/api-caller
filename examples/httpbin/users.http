@expand = profile

### Fetch a user
# @name get-user
# @step I fetch user {userId}
# @description Fetch a single user by id
# @assert status == 200
# @assert body.$.args.id == {{userId}}
# @assert header.content-type contains json
# @capture requestedId = body.$.args.id
GET {{baseUrl}}/get
    ?id={{userId}}
    &expand={{expand}}
Accept: application/json

### Create a user
# @name create-user
# @step a user named {user} is created
# @assert status == 200
# @assert body.$.json.name == {{user}}
POST {{baseUrl}}/post
Content-Type: application/json

{"name": "{{user}}", "email": "{{user}}@example.com", "created": "{{$isoTimestamp}}"}

### A request whose assertion expects a 404
# @name not-found
# @assert status == 404
GET {{baseUrl}}/status/404
