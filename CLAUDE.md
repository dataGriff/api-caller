See AGENTS.md for the repository layout, commands and conventions.

To try the tool against the example project without network access, run
`go run ./cmd/apic list -C examples/httpbin`; the `dev` environment targets
httpbin.org, so `run` needs outbound HTTPS.
